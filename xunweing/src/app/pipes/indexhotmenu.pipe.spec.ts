import { IndexhotmenuPipe } from './indexhotmenu.pipe';

describe('IndexhotmenuPipe', () => {
  it('create an instance', () => {
    const pipe = new IndexhotmenuPipe();
    expect(pipe).toBeTruthy();
  });
});
